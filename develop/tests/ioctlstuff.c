extern unsigned int __invalid_size_argument_for_IOC;
unsigned int __invalid_size_argument_for_IOC;




struct mtop
  {
    short int mt_op;
    int mt_count;
  };



static void
mtioctop (void)
{
  
   int tape;
    struct mtop mtop;
    const char *p;
//    off_t count = 0;
long long count;
    int negative;


    ((0) && (1)) ? 123: 456;
    printf("%d\n",  (1) + (2));
   puts((("gnu")));

    printf("%d\n",

    (((1U) << (((0 +8)+8)+14)) | ((('m')) << (0 +8)) | (((1)) << 0) | (((((sizeof(struct mtop) == sizeof(struct mtop[1]) && sizeof(struct mtop) < (1 << 14)) ? sizeof(struct mtop) : __invalid_size_argument_for_IOC)))))
    );

    printf("%d\n",

    ((1U << 30) | ((('m')) << (8)) | 1 | (((((sizeof(struct mtop) == sizeof(struct mtop[1]) && sizeof(struct mtop) < (1 << 14)) ? sizeof(struct mtop) : __invalid_size_argument_for_IOC)))))
    );

    printf("%d\n",
   			 ((1U << 30) | ((('m')) << (8)) | 1 | (((((sizeof(struct mtop) == sizeof(struct mtop[1]) && sizeof(struct mtop) < (1 << 14)) )))))
	  );

}

main() {
	mtioctop();
}

